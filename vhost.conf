<VirtualHost *:80>
    ServerName localhost
    DocumentRoot /var/www/app

    <Directory /var/www/app>
        Options -Indexes
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

